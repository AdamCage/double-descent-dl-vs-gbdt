Меня очень заинтересовала тема двойного спуска при bias-variance trade-off, а также особенность обучения градиентого бустинга, при которой наблюдается сперва выход на плато на трейне и валидации с последующим резким улучшением качества модели и спуском по logloss и на train и на val без переобучения.

Я немного исселдовал эту тему в @articles .

Хотелось бы исследовать её на более-менее реальном датасете: @data 

Там очень много строк (~500k), поэтому не пытайся самостоятельно его прочитать.

Хочется сделать скрипт-пайплайн, который будет:
1. читать данные (в идеале должно быть скачивания с kaggle, либо если уже есть нужный файл, то читаем)
2. предобрабатывать тексты
3. проводить первыйчный EDA и графики + артефакты сохранять в директорию result
3. извлекать эмбенденги из них с помощью эмбеддеров на ollama или vllm (по умолчанию qwen3-embedding:0.6b)
4. проводить анализ эмбендингов (UMAP + scatter 2d plot с разрезом по y_true) оригинальных данных + PCA 256 и анализ объясненной дисперсии, если 95% и более, то снижаем размерность до 128, если нет, то оставляем оригинал. Все графики / таблицы, если нужно тоже сохраняем.
5. Обучение глубокой нейросети с целью добиться эффекта двойного спуска bias-variance. Также максимальная визуализация, .json'ы, графики и т.д.
6. получение метрик + анализ изменения отсечки и метрики при разных вариантах отсечки (f1 best, ROC, Prevalance, ...), Также анализ распределений вероятностей на train / val / test, калибровка, UMAP + 2d scatter с окрасом по Proba и в разрезе Predict, y_true
7. Обучение catboost с параметрами, при которых удается достичь двойного спуска log-loss'а + максимально возможный подробный анализ именно этой части и того, как и из-за чего может происходить этот двойной спуск.
8. метрики, полностью аналогичные п.6
9 catboost vs Deep Learning сравнение (анализ распределений, метрик, сложности моделей, scatter-plot предсказанных вероятностей и т.д.)
10. Запись метаинформации (железо SEED, время, и т.д.)

---

Все это с максимально подробным логированием.

Графики - plotly с сохарнением .html (со всеми зависимостями для возможности запуска без интернета) + .png.

Для зависимостей используем uv.
